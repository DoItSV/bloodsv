<div class="container container__small">
  <h5>Inicia sesión para poder ingresar la información.</h5>
  <h6>Si aún no tienes una cuenta puedes crearla presionando el botón <strong>Crear cuenta</strong>. Es gratis.</h6>
  <div class='py-4'>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-group">
        <%= f.label :email, 'Email' %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: 'Email', required: '' %>
      </div>

      <div class="form-group">
        <%= f.label :password, 'Contraseña' %>
        <%= f.password_field :password, autocomplete: "current-password", class: 'form-control', placeholder: 'Contraseña', required: '' %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="form-inline">
          <div class="form-check">
            <%= f.check_box :remember_me, {:class => 'form-control'} %>
            <%= f.label :remember_me, 'Recordarme', {:class => 'ml-2'} %>
          </div>
        </div>
      <% end %>

      <div class="actions mt-2 text-center">
        <%= f.submit 'Iniciar sesión', class: 'btn btn-primary btn-block' %>
        <p class="mt-3">
          ¿No tienes cuenta?&nbsp;
          <a href="<%= new_registration_path(resource) %>">Crear cuenta</a>
        </p>
      </div>
    <% end %>
  </div>
</div>
